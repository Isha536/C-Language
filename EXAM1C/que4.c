#include<stdio.h>

void main()
{
	//program to count the digit
	int count,n;
	
	//for user input;
	printf("Enter a number: ");
	scanf("%d",&n);
	
	//counting the digit by for loop;
	for(count=0; n<=1; count++)
	{
		printf("Count= %d",count);
	}
}