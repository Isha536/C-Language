#include<stdio.h>
#include<conio.h>

void main()
{
	float length,rectangle;
	printf("Enter number for length of rectangle:");
	scanf("%f",&length);
	
	rectangle = length*length;
	printf("The length of the rectangle is %f",rectangle);
	
}